{
  "name": "@repo/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "dev:local": "tsx tests/dev-local-server.ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.2.0",
    "@repo/db": "workspace:*",
    "fastify": "^5.11.3",
    "pino": "^10.3.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitest/coverage-v8": "^3.2.0",
    "tsx": "^4.23.12",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
