{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["eslint.config.mjs", "tsconfig.base.json"],
  "tasks": {
    "db:generate": { "cache": false },
    "db:validate": { "cache": false },
    "build": {
      "dependsOn": ["^build", "^db:generate", "db:generate"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": { "dependsOn": ["^db:generate"], "cache": false, "persistent": true },
    "lint": {},
    "typecheck": { "dependsOn": ["^db:generate", "db:generate"] },
    "test": { "dependsOn": ["^db:generate", "db:generate"], "env": ["DATABASE_URL"] }
  }
}
